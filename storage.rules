rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read, write, and delete access for any authenticated user
    match /{allPaths=**} {
      allow read, write, delete: if request.auth != null;
    }
  }
}
